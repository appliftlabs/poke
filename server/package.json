{
  "name": "@applift/poke-server",
  "version": "0.1.0",
  "private": true,
  "description": "Reference backend for Poke — Postgres-backed, with SSE realtime.",
  "license": "MIT",
  "type": "module",
  "main": "src/server.js",
  "bin": {
    "poke-server": "src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
